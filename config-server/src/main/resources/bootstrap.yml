spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:///E:/LavoroMichele/workspace/Courses/Spring/config-server-repository
          clone-on-start: true
    fail-fast: true